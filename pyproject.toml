[project]
name = "Reelio"
version = "0.1.0"
description = "An app to help you find the best movies and music from reels"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
]

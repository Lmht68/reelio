[project]
name = "reelio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ThinhLe881", email = "thinhleminh201@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
reelio = "reelio:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[project]
name = "reelio"
version = "0.1.0"
description = "An AI-powered app that extracts books, music, and movie recommendations from reels"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.139.2",
]
